{
  "name": "check-coverage-check",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "package": "npx @vercel/ncc build src/index.ts -o dist",
    "clean": "rm -rf node_modules dist package-lock.json",
    "audit": "npm audit --audit-level=low || true"
  },
  "dependencies": {
    "@actions/core": "1.10.1",
    "@actions/github": "6.0.0",
    "fast-xml-parser": "4.4.1"
  },
  "devDependencies": {
    "@types/node": "20.12.12",
    "typescript": "5.4.5",
    "@vercel/ncc": "0.38.1"
  }
}
